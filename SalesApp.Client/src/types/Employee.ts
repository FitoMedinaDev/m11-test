export type Employee = {
    id: number
    createdAt: Date
    code: string
    name: string
    email: string
    documentNumber: number
    salary: number
    role: string
    documentType: DocumentType
}